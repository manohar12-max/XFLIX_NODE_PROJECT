import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Lato"
  },
  palette: {
    primary: {
      light: "#E5E5E5",
      main: "#181818",
      dark: "#181818",
      
    },
  },
});

export default theme;
